import type {MetadataRoute} from 'next'; import {articles,categories} from '@/lib/content'; export default function sitemap():MetadataRoute.Sitemap{const base='https://healthydeskhabits.com';return [{url:base,lastModified:new Date()},...categories.map(c=>({url:`${base}/${c.slug}`,lastModified:new Date()})),...articles.map(a=>({url:`${base}/article/${a.slug}`,lastModified:new Date(a.date)}))]}
