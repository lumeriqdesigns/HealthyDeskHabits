'use client'; import {useMemo,useState} from 'react'; import Link from 'next/link'; import {articles} from '@/lib/content';
export default function Search(){const [q,setQ]=useState(''); const results=useMemo(()=>articles.filter(a=>(a.title+' '+a.excerpt+' '+a.category).toLowerCase().includes(q.toLowerCase())),[q]); return <main className="container min-h-[65vh] py-16"><div className="eyebrow">Search HealthyDeskHabits</div><h1 className="serif mt-3 text-6xl tracking-[-.04em]">What are you looking for?</h1><label htmlFor="site-search" className="sr-only">Search articles</label><input id="site-search" name="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Try “eye strain”, “burnout”, “desk setup”…" className="mt-10 w-full rounded-2xl border border-[#dfe2dc] bg-white px-5 py-4 outline-none focus:ring-2 focus:ring-[#8faf9a]"/><div className="mt-10 grid gap-5">{results.map(a=><Link key={a.slug} href={'/article/'+a.slug} className="card p-6 hover:bg-white"><div className="eyebrow">{a.category}</div><h2 className="serif mt-2 text-2xl">{a.title}</h2><p className="mt-2 text-sm leading-6 text-[#737875]">{a.excerpt}</p></Link>)}{!results.length&&<p className="text-[#737875]">No articles found. Try another phrase.</p>}</div></main>}
