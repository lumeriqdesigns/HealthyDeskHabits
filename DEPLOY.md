# Deploy HealthyDeskHabits

1. Push these files so `package.json` is at the GitHub repo root (or set Vercel Root Directory to this folder).
2. Vercel Framework: Next.js
3. Build: `npm run build`
4. Redeploy after pushing this fixed version (duplicate reviewer + ESLint resolved).
